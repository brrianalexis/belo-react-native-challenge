import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  headerTitle: {
    textAlign: 'center',
    fontSize: 20,
    fontWeight: 'bold',
  },
  headerBalance: {
    textAlign: 'center',
    fontSize: 16,
  },
  errorMessage: {
    position: 'absolute',
    alignSelf: 'center',
    marginTop: 50,
    color: 'red',
  },
});

export default styles;
