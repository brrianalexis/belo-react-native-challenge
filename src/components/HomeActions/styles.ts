import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  actions: {
    flexDirection: 'row',
  },
  action: {
    padding: 30,
    alignItems: 'center',
  },
});

export default styles;
