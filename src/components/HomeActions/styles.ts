import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  actions: {
    flexDirection: 'row',
  },
  action: {
    padding: 30,
    alignItems: 'center',
  },
  icon: {
    fontSize: 30,
  },
});

export default styles;
