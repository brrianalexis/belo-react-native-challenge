import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  swapContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    marginTop: 40,
  },
  swapAmount: {
    color: 'purple',
    fontWeight: 'bold',
    fontSize: 35,
    marginVertical: 20,
    width: '75%',
  },
  swapMaxText: {
    fontSize: 20,
  },
});

export default styles;
