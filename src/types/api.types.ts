export type ExchangeRates = {
  bitcoin: {
    usd: number;
  };
  ethereum: {
    usd: number;
  };
  solana: {
    usd: number;
  };
  tether: {
    usd: number;
  };
  dai: {
    usd: number;
  };
};

export type DolarBlueRate = {
  fecha: string;
  compra: string;
  venta: string;
};
